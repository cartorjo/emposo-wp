{
  "wpBase": "http://localhost:8888",
  "themeBase": "/wp-content/themes/emposo",
  "allowedDeltas": [
    {
      "pattern": "^\\+<link href=\"/css/00-fonts\\.css\" rel=\"stylesheet\">$",
      "reason": "PLACEHOLDER — remove once the head is ported. Kept so the allowlist mechanism is exercised before phase 5.",
      "maxCount": 0
    }
  ],
  "note": "Every entry needs a reason and a maxCount. A delta that is not listed fails the gate; the list is printed in every report and reviewed in the PR that adds to it."
}
